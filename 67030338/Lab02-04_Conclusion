คำถามท้ายการทดลอง
1. นักศึกษาได้เรียนรู้อะไรบ้างเกี่ยวกับความแตกต่างและคุณสมบัติของชนิดข้อมูลแต่ละประเภทบน ESP32?

นักศึกษาได้เรียนรู้ว่าแต่ละชนิดข้อมูลมี ขนาดหน่วยความจำไม่เท่ากัน และมี ขอบเขตของค่าที่สามารถเก็บได้ เช่น:
	•	int และ long บน ESP32 มีขนาด 4 ไบต์ และสามารถเก็บค่าจำนวนเต็มได้ในช่วงประมาณ ±2 พันล้าน
	•	long long และ unsigned long long มีขนาด 8 ไบต์ เหมาะสำหรับเก็บค่าที่ใหญ่มาก
	•	float กับ double ต่างกันที่ความแม่นยำและขนาด (float = 4 ไบต์, double = 8 ไบต์)
	•	char เก็บอักขระเดียว และมีค่าที่สัมพันธ์กับรหัส ASCII
	•	bool เก็บค่าได้แค่ 1 (true) หรือ 0 (false)
	•	byte ใช้แค่ 1 ไบต์ และเก็บค่าจาก 0–255

2. ความสำคัญของการเลือกใช้ชนิดข้อมูลที่เหมาะสมในการเขียนโปรแกรมคืออะไร?

การเลือกชนิดข้อมูลที่เหมาะสมช่วยให้:
	•	ประหยัดหน่วยความจำ (สำคัญมากในระบบฝังตัวหรือไมโครคอนโทรลเลอร์)
	•	ป้องกันปัญหา overflow/underflow
	•	เพิ่มความเร็วในการประมวลผล
	•	ทำให้โปรแกรมอ่านง่ายและมีประสิทธิภาพมากขึ้น

3. ถ้าต้องการเก็บค่าเวลา (เป็นมิลลิวินาที) ที่อาจมีค่าสูงถึงหลายพันล้านมิลลิวินาที ควรใช้ชนิดข้อมูลใด?

✅ ควรใช้ unsigned long (32-bit) ถ้าเวลาไม่เกิน ~4,294,967,295 ms (~49.7 วัน)
✅ ถ้ามีโอกาสเกินกว่านั้น แนะนำให้ใช้ unsigned long long (64-bit) เพื่อรองรับค่ามากกว่า

4.อธิบายความแตกต่างระหว่าง float และ double ในแง่ของขนาดหน่วยความจำและความแม่นยำ
 -float มีขนาด 4 ไบต์ และมีความแม่นยำ 6-7 ตำแหน่งทศนิยม
  -double มีขนาด 8 ไบต์ และมีความแม่นยำ -15 ตำแหน่งทศนิยม
5. อธิบายแนวคิดเรื่อง “Overflow” และ “Underflow” ที่เกิดขึ้นกับชนิดข้อมูลจำนวนเต็ม พร้อมยกตัวอย่าง
	•	Overflow: เกิดเมื่อค่าที่กำหนดเกินขอบเขตบนของชนิดข้อมูล
🔸 เช่น int myInt = 2147483647 + 1; → ได้ค่ากลับเป็น -2147483648
	•	Underflow: เกิดเมื่อค่าที่กำหนดต่ำกว่าขอบเขตล่างของชนิดข้อมูล
🔸 เช่น byte myByte = 0 - 1; → ได้ค่า 255 เพราะ byte เก็บได้แค่ 0–255

6. การทราบขนาดของชนิดข้อมูลด้วย sizeof() มีประโยชน์อย่างไร?
	•	ช่วยให้สามารถ บริหารจัดการหน่วยความจำได้อย่างมีประสิทธิภาพ โดยเฉพาะในระบบที่มีหน่วยความจำจำกัด เช่น ESP32
	•	ลดโอกาสเกิดปัญหา overflow/underflow
	•	ใช้ในการเขียนโค้ดที่ยืดหยุ่น รองรับหลายสถาปัตยกรรม (portable code)
	•	ใช้ในโครงสร้างข้อมูล เช่น การจัดตำแหน่ง memory หรือ serialization
